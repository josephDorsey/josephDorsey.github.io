"use strict";

const btn_StartWorkout = document.querySelector(".btn--startWorkout");
btn_StartWorkout.addEventListener("click", function () {
  // for (let i = 0; i < exerciseGroup.name.length; i++) {
  //   labelExercise.innerHTML = `Exercise: ${exerciseGroup.name[i]}`;
  //   labelWeight.innerHTML = `Weight: ${exerciseGroup.weight[i]}-lbs`;
  //   labelSets.innerHTML = `Set: 1 / ${exerciseGroup.sets.max[i]}`;
  //   labelReps.innerHTML = `Reps: ${exerciseGroup.reps[i]}`;
  //   labelRest.innerHTML = `Rest: ${exerciseGroup.rest.minutes[i]}m ${exerciseGroup.rest.seconds[i]}s`;
  // }
});
